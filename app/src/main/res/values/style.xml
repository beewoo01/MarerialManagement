<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Widget.MaterialManagement.Toolbar" parent="Widget.MaterialComponents.Toolbar.Primary">
        <item name="android:background">@color/sub_color</item>
        <item name="android:title">@null</item>
    </style>

    <style name="Widget.MaterialManagement.Toolbar.WithArrow" parent="Widget.MaterialManagement.Toolbar">
        <item name="navigationIcon">@drawable/ic_previous</item>
        <item name="android:background">@color/sub_color</item>
        <item name="title">@null</item>

    </style>

    <style name="CustomBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/CustomBottomSheetStyle</item>
    </style>

    <style name="CustomBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="Widget.MaterialManagement.EditText" parent="Widget.AppCompat.EditText">
        <item name="android:textColorHint">@color/font_gray_color</item>
    </style>

    <style name="BottomNavigation" parent="Widget.Design.BottomNavigationView">
        <item name="android:background">@color/white</item>
        <item name="itemTextColor">@color/font_gray_color</item>
        <item name="android:textColor">@color/font_gray_color</item>
        <item name="tabSelectedTextColor">@color/main_color</item>
    </style>

    <style name="FullScreenDialogStyle" parent="Theme.AppCompat.Dialog">

        <item name="android:windowNoTitle">true</item>
        <item name="colorPrimaryDark">@color/main_color</item>
        <item name="colorPrimary">@color/sub_color</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>

        <!-- Set this to true if you want Full Screen without status bar -->
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowIsFloating">false</item>

        <!-- This is important! Don't forget to set window background -->
        <item name="android:windowBackground">@color/white</item>

    </style>

    <style name="AnimationPopupStyle">

        <item name="android:windowEnterAnimation">@anim/dialog_fade_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_fade_out</item>
    </style>

    <style name="SpinnerTheme" parent="android:Widget.Spinner">
        <item name="android:background">@drawable/bg_simple_spinner</item>
    </style>

    <!--<style name="Widget.MaterialManagement.Toolbar.WithClose" parent="Widget.chapter01.Toolbar">
        <item name="android:navigationIcon">@drawable/ic_close</item>
        <item name="android:title">@null</item>
    </style>-->
</resources>